version: 1
applications:
  - appRoot: posenet-app
    frontend:
      phases:
        preBuild:
          commands:
            - nvm use 12
            - npm install
            - npm run lint
            - npm run test
        build:
          commands:
            - nvm use 12
            - npm run build
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'